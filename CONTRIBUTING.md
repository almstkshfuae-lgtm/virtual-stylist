# Contributing

If you make changes locally, please:

```bash
npm install
git add package-lock.json
git commit -m "chore: add package-lock.json"
git push
```

This repository expects a lockfile for reproducible builds. If you don't have one, run `npm install` to create `package-lock.json` before opening a PR.
