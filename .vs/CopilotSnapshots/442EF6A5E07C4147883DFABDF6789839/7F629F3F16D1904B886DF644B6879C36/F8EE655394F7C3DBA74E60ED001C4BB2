@import "tailwindcss";

html,
body {
  width: 100%;
  min-height: 100%;
}

body {
  overscroll-behavior: smooth;
  overflow-x: hidden;
}

#root {
  min-height: 100vh;
  width: 100%;
}

:root {
  --brand-burgundy: #6b1a3c;
  --landing-cream: #f7f1e8;
  --landing-ink: #141318;
  --landing-accent: #c25b3e;
  --landing-moss: #1f4e3d;
  --landing-rose: #8c2340;
  --hero-bg-fallback: url('/hero_section_background.png');
  --hero-bg-image: image-set(
    url('/hero_section_background.avif') type('image/avif') 1x,
    url('/hero_section_background.webp') type('image/webp') 1x,
    url('/hero_section_background.png') type('image/png') 1x
  );
}

html.dark {
  --landing-cream: #0f1115;
  --landing-ink: #f7f1e8;
  --landing-accent: #e27a55;
  --landing-moss: #7fd0b8;
  --landing-rose: #f06a8c;
}

@media (max-width: 768px) {
  :root {
    --hero-bg-fallback: url('/hero_section_background_768.png');
    --hero-bg-image: image-set(
      url('/hero_section_background_768.avif') type('image/avif') 1x,
      url('/hero_section_background_768.webp') type('image/webp') 1x,
      url('/hero_section_background_768.png') type('image/png') 1x
    );
  }
}

@media (min-width: 769px) and (max-width: 1280px) {
  :root {
    --hero-bg-fallback: url('/hero_section_background_1280.png');
    --hero-bg-image: image-set(
      url('/hero_section_background_1280.avif') type('image/avif') 1x,
      url('/hero_section_background_1280.webp') type('image/webp') 1x,
      url('/hero_section_background_1280.png') type('image/png') 1x
    );
  }
}

.text-brand {
  color: var(--brand-burgundy);
}

.bg-brand {
  background-color: var(--brand-burgundy);
}

.bg-hero-image {
  background-image: var(--hero-bg-fallback);
  background-image: var(--hero-bg-image);
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  filter: saturate(0.35) brightness(0.55);
}

.hero-hello {
  isolation: isolate;
}

.hero-hello::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 20% -10%, rgba(255, 255, 255, 0.08), transparent 45%),
    linear-gradient(180deg, rgba(6, 9, 19, 0.55), rgba(3, 5, 12, 0.85));
  z-index: 10;
  opacity: 0.95;
  pointer-events: none;
}

.gemini3-logo {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  background: linear-gradient(90deg, #5c6ef8, #7c3aed, #ec4899);
  -webkit-background-clip: text;
  color: transparent;
  display: inline-flex;
  align-items: center;
  gap: 0.2rem;
}

.gemini3-logo-number {
  font-size: 0.9rem;
  color: inherit;
  opacity: 0.95;
}

.clip-dark-ltr {
  clip-path: inset(0 58.333% 0 0);
}

.clip-dark-rtl {
  clip-path: inset(0 0 0 58.333%);
}

.clip-light-ltr {
  clip-path: inset(0 0 0 41.667%);
}

.clip-light-rtl {
  clip-path: inset(0 41.667% 0 0);
}

/* For custom scrollbar styling if needed */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: #f1f1f1;
}

::-webkit-scrollbar-thumb {
  background: #888;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: #555;
}

html.dark ::-webkit-scrollbar-track {
  background: #2d3748; /* dark:bg-gray-700 */
}

html.dark ::-webkit-scrollbar-thumb {
  background: #718096; /* dark:bg-gray-500 */
}

html.dark ::-webkit-scrollbar-thumb:hover {
  background: #a0aec0; /* dark:bg-gray-400 */
}

@keyframes fade-in-up {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-up {
  animation: fade-in-up 0.6s ease-out forwards;
}

.animation-delay-300 {
  animation-delay: 0.3s;
  opacity: 0; /* Start hidden */
}

@keyframes background-pan {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.animate-background-pan {
  background-size: 400% 400%;
  animation: background-pan 15s ease infinite;
}

@keyframes fade-in-stagger {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.animate-fade-in-stagger {
  opacity: 0;
  animation: fade-in-stagger 0.5s ease-out forwards;
}

@keyframes shimmer {
  100% { transform: translateX(100%); }
}

@keyframes text-fade-in {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-text-fade-in {
  animation: text-fade-in 0.8s ease-out forwards;
  opacity: 0; /* Start hidden */
}

@keyframes gentle-bounce {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-5px);
  }
}

.hover\:animate-gentle-bounce:hover {
  animation: gentle-bounce 0.5s ease-in-out;
}

@keyframes smooth-scroll {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(8px);
  }
}

.animate-smooth-scroll {
  animation: smooth-scroll 2s ease-in-out infinite;
}

/* Responsive text sizing utility */
@media (max-width: 640px) {
  .text-responsive-hero {
    font-size: clamp(1.875rem, 8vw, 2.25rem);
  }
}

@media (min-width: 641px) and (max-width: 768px) {
  .text-responsive-hero {
    font-size: clamp(2.25rem, 6vw, 3rem);
  }
}

@media (min-width: 769px) {
  .text-responsive-hero {
    font-size: clamp(2.25rem, 5vw, 4rem);
  }
}

.text-responsive-subtitle {
  font-size: clamp(1rem, 3vw, 1.35rem);
}

/* Focus visible for accessibility */
.focus-visible-ring:focus-visible {
  outline: 2px solid currentColor;
  outline-offset: 2px;
}

@keyframes item-pop-in {
  from {
    transform: scale(0.8);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}

.animate-item-add {
  animation: item-pop-in 0.3s ease-out forwards;
}

@keyframes item-pop-out {
  from {
    transform: scale(1);
    opacity: 1;
  }
  to {
    transform: scale(0.8);
    opacity: 0;
  }
}

.animate-item-remove {
  animation: item-pop-out 0.3s ease-out forwards;
}

@keyframes letter-reveal {
  0% {
    opacity: 0;
    transform: translateY(40px) scale(0.5);
    filter: blur(8px);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

.animate-letter-reveal {
  display: inline-block;
  opacity: 0;
  animation: letter-reveal 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  will-change: transform, opacity, filter;
}

@keyframes image-subtle-scale {
  from {
    opacity: 0;
    transform: scale(1.1);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.animate-image-scale {
  animation: image-subtle-scale 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
}

/* Landing page styling */
.landing-shell {
  position: relative;
  isolation: isolate;
  background:
    radial-gradient(900px circle at 8% 12%, rgba(194, 91, 62, 0.18), transparent 55%),
    radial-gradient(800px circle at 88% 18%, rgba(31, 78, 61, 0.16), transparent 60%),
    radial-gradient(900px circle at 40% 92%, rgba(140, 35, 64, 0.14), transparent 60%),
    linear-gradient(180deg, var(--landing-cream) 0%, #ffffff 68%, #ffffff 100%);
  color: var(--landing-ink);
}

.landing-shell::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(12, 12, 12, 0.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(12, 12, 12, 0.06) 1px, transparent 1px);
  background-size: 24px 24px;
  opacity: 0.22;
  pointer-events: none;
  z-index: 0;
}


html.dark .landing-shell {
  background:
    radial-gradient(900px circle at 10% 12%, rgba(226, 122, 85, 0.2), transparent 55%),
    radial-gradient(800px circle at 90% 15%, rgba(127, 208, 184, 0.18), transparent 60%),
    radial-gradient(800px circle at 40% 90%, rgba(240, 106, 140, 0.16), transparent 60%),
    linear-gradient(180deg, var(--landing-cream) 0%, #0b0e12 68%, #0b0e12 100%);
  color: var(--landing-ink);
}

html.dark .landing-shell::before {
  background-image:
    linear-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.08) 1px, transparent 1px);
}

.landing-shell,
.landing-shell * {
  font-family: 'Rubik', 'Segoe UI', system-ui, -apple-system, sans-serif;
}

html[dir="rtl"] .landing-shell,
html[dir="rtl"] .landing-shell * {
  font-family: 'Tajawal', 'Segoe UI', system-ui, -apple-system, sans-serif;
}

.landing-hero {
  position: relative;
  z-index: 1;
}

.landing-hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(60% 60% at 78% 8%, rgba(255, 255, 255, 0.5), transparent 60%),
    var(--hero-bg-fallback);
  background-image:
    radial-gradient(60% 60% at 78% 8%, rgba(255, 255, 255, 0.5), transparent 60%),
    var(--hero-bg-image);
  background-size: cover;
  background-position: center;
  opacity: 0.22;
  filter: saturate(0.9) contrast(1.05);
  pointer-events: none;
}

html.dark .landing-hero::before {
  opacity: 0.16;
  filter: saturate(0.8) contrast(1.1);
}

.landing-hello {
  position: absolute;
  top: -1.5rem;
  left: 50%;
  transform: translateX(-50%);
  font-size: clamp(3rem, 14vw, 10rem);
  font-weight: 800;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: rgba(15, 15, 15, 0.08);
  z-index: 0;
  pointer-events: none;
  white-space: nowrap;
}

html.dark .landing-hello {
  color: rgba(248, 242, 234, 0.08);
}

html[dir="rtl"] .landing-hello {
  letter-spacing: 0;
}

.landing-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  border-radius: 999px;
  border: 1px solid rgba(15, 15, 15, 0.1);
  padding: 0.4rem 0.9rem;
  font-size: 0.8rem;
  font-weight: 600;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(8px);
}

html.dark .landing-eyebrow {
  border-color: rgba(255, 255, 255, 0.2);
  background: rgba(15, 18, 24, 0.6);
}

html[dir="rtl"] .landing-eyebrow {
  letter-spacing: 0;
}

.landing-card {
  border-radius: 1.5rem;
  border: 1px solid rgba(15, 15, 15, 0.12);
  background: rgba(255, 255, 255, 0.78);
  box-shadow: 0 25px 60px rgba(20, 20, 20, 0.15);
  backdrop-filter: blur(10px);
}

html.dark .landing-card {
  border-color: rgba(255, 255, 255, 0.16);
  background: rgba(15, 18, 24, 0.7);
  box-shadow: 0 25px 60px rgba(0, 0, 0, 0.45);
}

.landing-chip {
  display: inline-flex;
  align-items: center;
  gap: 0.45rem;
  border-radius: 999px;
  border: 1px solid rgba(15, 15, 15, 0.12);
  padding: 0.35rem 0.7rem;
  font-size: 0.75rem;
  font-weight: 600;
  background: rgba(255, 255, 255, 0.72);
  color: var(--landing-ink);
}

html.dark .landing-chip {
  border-color: rgba(255, 255, 255, 0.18);
  background: rgba(15, 18, 24, 0.7);
  color: var(--landing-ink);
}

/* Very small phones (320-360px): tighten spacing and keep controls visible. */
@media (max-width: 360px) {
  .landing-header-panel {
    top: 0.5rem;
    left: 0.4rem;
    right: 0.4rem;
    padding: 0.45rem;
    gap: 0.4rem;
    border-radius: 1rem;
  }

  .landing-header-row {
    gap: 0.4rem;
  }

  .landing-restore-row {
    padding: 0.4rem;
    gap: 0.4rem;
  }

  .landing-hero {
    padding-top: 12.5rem !important;
    padding-bottom: 2.5rem !important;
  }

  .landing-hello {
    font-size: clamp(2.3rem, 16vw, 4rem);
    top: -0.8rem;
  }

  .app-header {
    padding: 0.6rem 0.75rem;
  }

  .app-main {
    padding: 0.75rem;
  }
}

/* Landscape phones: reduce vertical overhead and keep workflows in view. */
@media (max-height: 500px) and (orientation: landscape) {
  .landing-header-panel {
    top: 0.4rem;
    max-height: calc(100vh - 0.8rem);
    padding-top: 0.45rem;
    padding-bottom: 0.45rem;
  }

  .landing-hero {
    padding-top: 9.5rem !important;
    min-height: 62vh !important;
  }

  .landing-hello {
    font-size: clamp(2.2rem, 10vw, 4.5rem);
  }

  .uploader-dropzone {
    height: 10.5rem;
  }

  .app-header {
    padding-top: 0.5rem;
    padding-bottom: 0.5rem;
  }
}
